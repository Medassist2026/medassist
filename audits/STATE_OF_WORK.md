# MedAssist — State of Work
> Living document. Updated at the START and END of every cowork session.
> Source of truth for "what are we working on, what's done, what's next, what's blocked."

**Last updated:** 2026-05-09 (Phase F closeout autonomous batch: 3 of 4 tasks shipped — Task 1 (`.husky/pre-push` extended 3 → 5 passes adding `next build` clinic + patient as Pass 4/5, closing Lesson #17's operational follow-up); Task 2 (Tier 2 Dependabot — apps' next 14.2.25 → 14.2.35 + paired eslint-config-next bump; lockfile delta 450 lines bounded to next dep tree + dedupe; 21 expected closures per Decision 8); Task 4 (Phase F Task 19 root next AUDITED — vestigial template residue, removal DEFERRED to focused workstream Task 19a). Task 3 (Phase F Task 16 Phase 3 TS literal-union refactor) DEFERRED to a focused session per Decision 11 — defense-in-depth not regressed because yesterday's eslint static-string discipline rule already enforces the precondition. Sandbox constraint: `next build` exceeds 45s sandbox timeout; gate-5 verification Mac-deferred per Decision 4. 12 decisions logged in `audits/phase-f-closeout-decisions-2026-05-09.md`. **Mac-side cleanup before commit:** `rm -rf apps/clinic/app/api/_lesson17_test`.)
**Last session:** Phase F closeout autonomous batch (4 originally-scoped tasks; 3 shipped + 1 deferred). Authority model: cowork session executed autonomously per the protocol-prompted "surface ONCE at end" pattern, using REVIEW_CRITERIA.md §2 as authority for trade-off decisions. Pre-work verification: 8/8 premises verified (HEAD = `1a97dd5`; CI all 3 checks SUCCESS at `1a97dd5` per GitHub public REST API since `gh` was not available in sandbox; open Dependabot alerts = 50; REVIEW_CRITERIA §1.1/§2.1/§5 present; Lesson #18 codified at line 309 of EXECUTION_PROMPTS.md; ALLOWED_ADMIN_SCOPES = 136; eslint rule wired; .husky/pre-push has 3 gates pre-bump). Pre-bump local-gate dry-run on HEAD: gates 1-4 all clean. **Task 1 ✅:** `.husky/pre-push` extended; 5-pass model documented in inline header; ARCH §3 file-tree comment + technology stack pre-push row updated to reflect 3 → 5 passes. Empirical sandbox demonstration of Lesson #17 anti-pattern: with broken handler `GET(request?: Request)` in place, gates 1-3 (root tsc + clinic tsc + lint:scopes) all pass clean — empirically reproducing Lesson #17's central observation that `tsc --noEmit` does not enforce the route-handler contract; only `next build` does. Test fixture left at `apps/clinic/app/api/_lesson17_test/route.ts` (sandbox `rm` blocked; overwritten with `export {}` stub + DELETE comment per Phase F Task 20 precedent / Decision 5). **Task 2 ✅:** Apps' next + eslint-config-next bumped 14.2.25 → 14.2.35; lockfile delta 450 lines (36+, 422-) bounded entirely to next dep tree (`@next/env`, `@next/eslint-plugin-next`, `@next/swc-*` platform binaries, etc.) + dedupe cleanup of pre-existing peer:true / license-trailing-comma drift from npm 11 vs npm 10 metadata refresh. Gates 1-4 clean post-install (G1 3.8s + G2 3.1s + G3 2.3s + G4 4.4s ≈ 13.6s wall). Gate 5 (next build × 2) Mac-deferred. **Expected closures: 21** = 14 manifest closures (7 GHSAs × 2 apps: GHSA-5j59 + GHSA-mwv6 + GHSA-4342 + GHSA-xv57 + GHSA-g5qg + GHSA-3h52 + GHSA-223j) + 7 lockfile closures (same 7 GHSAs once 14.2.25 leaves the lockfile). Per Lesson #18, post-push verification query embedded in Decision 8 (filter by `fixed_at >= 2026-05-09T06:11:48Z`). **Task 4 ✅:** Phase F Task 19 root next AUDITED — categorized as vestigial template residue (referenced by 4 dead scripts `dev/build/start/lint` from `4a4f368` "initial upload"; no `app/`/`pages/`/`next.config.js`/imports at root; turbo.json uses workspace dependency chains; CI workspace-scoped; no vercel.json; 0 root-package.json next alerts). Removal DEFERRED to **new workstream Task 19a — root vestigial dep cleanup** which scopes triage of all root deps with template-residue origin (next + Radix + lucide-react + framer-motion + cmdk) into one focused PR with Vercel preview deploy verification. **Task 3 ⏸ DEFERRED:** Phase F Task 16 Phase 3 TS literal-union refactor — the largest task (90-150 min); defense-in-depth not regressed (yesterday's eslint rule covers the security gap); approach selection (1: `as const` tuple → derived union; 2: manual literal union; 3: branded type) + 135-callsite verification + deliberate-violation tests deserve dedicated session capacity. Handoff state captured in Decision 11. **Lockstep doc updates landed (Decision 12 scope):** ARCH §3 (apps next pin 14.2.25 → 14.2.35 + root vestigial-residue annotation), ARCH §16 (file tree pre-push comment + technology stack pre-push row updated to 3 → 5 passes), DECISIONS_LOG.md (Phase F closeout entries appended), this STATE_OF_WORK.md header + Last session + workstream Active → Completed move, audits/PROGRAM_STATE.md (Tasks 19/Tier 2/pre-push gate enhancement → DONE/AUDITED entries 13 + 15). **Out-of-scope doc updates DEFERRED to Task 3 focused session** (per Decision 12): ARCH §12 admin scope mechanics narrative (Task-3-shaped), D-008 fourth amendment (Phase 3 ship-doc), EXECUTION_PROMPTS new lesson (only emerges from Task 3 if applicable), audits/admin-scope-reconciliation-2026-05-08.md §10 (Phase 3 receipt section).

**Prior session (Triage doc empirical correction + Phase F Task 20 batch — kept for reference):** Multi-task batch (Task 1 + Task 2) covering Dependabot triage doc empirical correction + Phase F Task 20 (Option A expansion + eslint static-string discipline rule). **Task 1:** Added §0.5 (Empirical correction) + §0.6 (Lesson #18 candidate) to `audits/dependabot-triage-2026-05-08.md`. Empirical findings: D1's predicted-7 closures all fired exactly as predicted (`fixed_at = 2026-05-09T03:15:29Z`, ~7 hour recheck latency from push). Net delta of -5 (55 → 50) was 7 closures + 2 post-snapshot arrivals (`fast-uri v39h` + `@babel/plugin-transform-modules-systemjs fv7c`, both at `2026-05-09T03:15:30Z`) — or 7 + 3 if counting `fast-uri q3j6` from yesterday's broader push-to-recheck window. Manifest-double-counting hypothesis empirically confirmed for GHSA-5j59 + GHSA-mwv6 with one nuance: each has 4 alert rows (not 3), and the root `package.json` entries closed on 2026-05-07 via a Dependabot tracking-model migration (NOT our work) — bumping root next on 2026-05-08 closed only the workspace lockfile entries for advisories whose vulnerable range stops below apps' 14.2.25; for the multi-manifest GHSAs (5j59 / mwv6 / etc.), the workspace lockfile entry stays open until apps bump too (Tier 2 workstream). **Task 2:** `ALLOWED_ADMIN_SCOPES` expanded 35 → 136 entries (105 missing added in 16 feature blocks with comment dividers; 4 truly-unused removed: `api-versioning`, `input-validation`, `phone-change-rollback`, `privacy-migration-backfill`; the "132" target in the 2026-05-08 reconciliation doc was a math error — correct arithmetic 35 + 105 − 4 = 136). Custom eslint rule `eslint-rules/no-unregistered-admin-scope.js` + plugin packaging (`eslint-rules/index.js` + `eslint-rules/package.json` + root `package.json` `file:` linkage + `.eslintrc.json` plugin/rule wiring) + new `lint:scopes` script. Three rule classes empirically validated by smoke tests against contrived bad commits (unregistered scope / template literal / variable arg all fire correctly with their dedicated message IDs; clean cases pass). Five verification gates clean: `lint:scopes`, three tsc gates, per-app `next lint` clinic + patient. **Lesson #18 codified** in `audits/EXECUTION_PROMPTS.md` (filter by event timestamp, not net delta, when measuring closures) + `audits/REVIEW_CRITERIA.md` §2.1 verification-table closure-counts row pointing at Lesson #18. **Lockstep doc updates landed:** ARCH §12 (count 35 → 136 + reconciliation pointer + Phase 2 narrative), D-008 Amendment 2026-05-09 appended (third amendment — Phase 2 shipped with full mechanism description), reconciliation doc §9 appended (Phase 2 shipped section + math correction explanation), EXECUTION_PROMPTS Lesson #18, REVIEW_CRITERIA §2.1, PROGRAM_STATE Task 20 → DONE, this STATE_OF_WORK header + Last session + workstream Active → Completed move + Phase F summary list refresh. **Out-of-scope finding:** smoke test left a scratch file at `apps/clinic/app/__rule-test-bad-scope.tsx` (sandbox `rm` couldn't delete it due to mount permissions; replaced content with a benign `export {}` stub flagged "DELETE BEFORE COMMIT"). Surfaced for Mo to delete before the batch commit.

**Prior session (Phase F Task 16 multi-task batch, kept for reference):** Multi-task batch (D1+D2+D3) covering Dependabot triage, critical alert remediation, and Phase F Task 16 admin-scope reconciliation. **D1:** Triaged 55 open Dependabot alerts (29 unique GHSAs after manifest dedup); 1 critical / 22 high / 25 medium / 7 low; 8 GHSAs real-blocking with clean version-bump fix (Tier 1 — D2 candidate), 5 GHSAs require Next 15 major bump (Tier 3 — deferred), 16 GHSAs theoretical-build-pipeline (Tier 4 — quarterly dep-update sprint). Pre-work drift surfaced (54 → 55, +1 high alert `fast-uri ≤ 3.1.0` created during this very session) — prompt's "surface and continue" rule held. **D2:** Pragmatic root next bump 14.1.0 → 14.2.35 (closes critical CVE-2025-29927 + 6 root-only `next` advisories at zero extra blast radius within 14.2.x). Lockfile delta bounded entirely to next dep tree (113 lines, 54/61 on Mac canonical install). Three tsc gates clean; both apps' `next build` passed locally (Lesson #17 gap closed empirically — route-handler contracts validated via real build, not just tsc). `npm audit --json` post-bump: critical 1 → 0 ✓. Apps' next 14.2.25 untouched — Tier 2 (apps bump to 14.2.35) is a separate workstream. **D3:** Inventoried admin-scope drift end-to-end. Precise counts: 210 total `createAdminClient(...)` invocations (207 explicit-arg + 3 no-arg defaulting to `'api-route'`); 135 unique scope strings; 35-entry allow-list (precise; supersedes "~36" approximation); 105 missing; 4 truly-unused; 0 typos / near-misses; 0 dynamic template-literal callsites; uniform kebab-case naming; 100% of missing scopes in production code paths. Largest cluster: Build 05 patient-sharing lifecycle (D-068), 18 of 105. Recommended Option D (sequenced hybrid): Phase 1 (this batch — TRIAGED only), Phase 2 (Task 20: allow-list expansion to 132 entries + MANDATORY eslint static-string discipline rule), Phase 3 (Option C.1 TypeScript literal-union refactor). Option B likely DROPPED. JSON dumps committed as raw evidence per ruling; `/tmp/*.tsv` working files NOT committed (codebase state is git-versioned). Lockstep doc updates landed: ARCH §12, D-008 dual amendments, PROGRAM_STATE Tasks 12/13/14, this STATE_OF_WORK header + workstream lifecycle. Files: `audits/dependabot-triage-2026-05-08.md` (new, 379 lines), `audits/dependabot-alerts-2026-05-08.json` (new, 55 alerts × ~3.7KB each), `audits/ci-runs-2026-05-08.json` (new), `audits/admin-scope-reconciliation-2026-05-08.md` (new, ~370 lines after additions), `package.json` (next 14.1.0 → 14.2.35), `package-lock.json` (113-line bounded delta), `ARCHITECTURE.md`, `DECISIONS_LOG.md`, `audits/PROGRAM_STATE.md`, `audits/STATE_OF_WORK.md` (header + workstream Active → Completed move).

**Prior session (REVIEW_CRITERIA.md authoring, kept for reference):** Author REVIEW_CRITERIA.md — externalized review framework. Codifies Mo's review checks (pre-work claim verification, mid-work checkpoints, surface format, failure-mode catalog, push-back authorization) into a versioned doc cowork sessions self-check against before surfacing. Pre-work verification gate caught a real bug in STATE_OF_WORK.md line 125 (`5ad4003` was an unreachable orphan from a `git commit --amend`; actual HEAD is `0abce28`). Per Path 2 ruling, fix bundled into this commit using the same annotation pattern line 122 used for `d8daa60 → bad1100`. Doc structure: 6 sections (pre-work / mid-work / surface / failure-modes / push-back / maintenance); 6 failure-mode patterns in §4 each backed by concrete examples from this project's history (5 from doc-verification-sweep + 1 from session history); 476 lines, well under the ~600 cap. Lockstep peers updated: STATE_OF_WORK.md (this entry + Session protocol + Methodology rules), EXECUTION_PROMPTS.md (Empirical lessons preamble points to REVIEW_CRITERIA.md as the operational distillation), ARCHITECTURE.md §16 (lockstep doc set note appended).

**Prior session (Phase F Task 10, kept for reference):** Phase F Task 10 (P0) — admin scope addition. Verified at the call site (`packages/shared/lib/api/handlers/frontdesk/checkin/handler.ts:145`) that the scope string passed to `createAdminClient` is exactly `'auto-renew-on-visit-gpid-lookup'` — matched the task description verbatim. Confirmed scope was genuinely absent from `ALLOWED_ADMIN_SCOPES`; no near-duplicates of the same logical scope in the file. Added a single line under a new feature-grouped comment block (`// ── Patient sharing lifecycle (mig 090 / Build 05 § B7 — D-068) ──`) keeping kebab-case convention. Three tsc gates clean (root + per-app patient + per-app clinic). **Out-of-scope finding surfaced for Task 16 (planned, P1):** `packages/shared/lib/data/patient-shares.ts:332` uses `createAdminClient('patient-shares-auto-renew')` — also missing from the allow-list, but a different logical scope (the share renewal RPC inside `autoRenewOnVisit`, distinct from the GPID lookup at the handler call site); not addressed here per the workstream's explicit "ONE specific scope" boundary. **Doc updates:** ARCHITECTURE.md §12 (`~35 → ~36` in allow-list), D-008 Amendment 2026-05-04 appended with closure note, PROGRAM_STATE.md (new Task 10 entry as ✅ DONE), STATE_OF_WORK.md header + Active→Completed move + bookkeeping fix.

**Prior session context (Phase F Task 18, kept for reference):** Fix `rls_test_teardown()` audit_events cleanup. D-074's run #1.5 surfaced that PCR audit rows accumulate +4 per seed cycle because the teardown's audit_events DELETE clause only matched `entity_type IN ('global_patients', 'patient_data_share')`. Investigation pulled live function body from `pg_proc` (Lesson #7) — confirmed staging body is a SUPERSET of `audits/rls-test-seed.sql` (extra ops/comm + clinical-data cleanup blocks added in Sessions 11 + 14a; the file is out of date). Verified `audit_events.resolved_global_patient_id` is a GENERATED ALWAYS column (computed from `metadata->>'global_patient_id'` or `entity_id` when `entity_type='global_patients'`) — no resolver-trigger timing concern. Authored mig 108 + rollback: `CREATE OR REPLACE FUNCTION public.rls_test_teardown()` with the live body verbatim plus two new audit_events DELETE clauses (`resolved_global_patient_id = ANY(test_gps)` master clause + defensive `entity_type='patient_clinic_record' AND metadata->>'global_patient_id' = ANY(test_gps)` for readability). Smoke probe inserts a sentinel PCR audit row with `metadata.global_patient_id` set to `patient_y_gp` (cannot directly INSERT the generated column), calls teardown, asserts the sentinel + 40 pre-existing accumulated PCR rows are all cleaned. Mig 108 applied 2026-05-07 (smoke = PASS). Re-ran the matrix at `run_no = 1.6` against staging post-teardown+seed: **177/177 PASS, fail=0 across all 24 tables**, `audit_events.S6 = 1` (the post-fix cycle-stable invariant; was 8 in run #1, 10 in run #1.5). Comparison-vs-run-#1 query returns exactly one divergence row (audit_events.S6 row count 8→1, outcome SUCCESS=SUCCESS preserved) — that's the expected post-fix invariant per D-074 amendment. Files touched: `supabase/migrations/108_fix_rls_test_teardown_audit_events.sql` + `.rollback.sql` (new), `audits/rls-test-matrix-reconstructed.sql` (run_no 1.5→1.6 in 24 DO blocks + Section 0; audit_events.S6 dynamic→hardcoded 1; Section 25 validation queries updated; new D-074 amendment commentary in Section 19), `ARCHITECTURE.md` (§3 row count 110→111 + highest base 107→108; §8.6 mig 108 row added), `DECISIONS_LOG.md` (D-074 amendment blockquote added after Outcome — D-004/D-005/D-008 pattern), `audits/STATE_OF_WORK.md`, `audits/PROGRAM_STATE.md`.

**Parallel session today (CI run 25535878629 root cause + fix):** Investigation of CI run 25535878629 (failure on commit `134e272`, doc-only). Failed job: **Build Patient App** step 5, exit 1, 26s. Type-check + clinic build pass. Investigation surfaced this is the SIXTH consecutive failure (runs 115–120 since `19fb148` on 2026-05-07 03:38 UTC; last green = `778467a` run 114) — every red run failed at the same step. **Mid-session, Mo uploaded the `patient-build-log` artifact contents from run 25535878629**, which exposed the actual error: `packages/shared/lib/api/handlers/patient/sharing/handler.ts` line 27 declared `export async function GET(request?: Request)`. The optional `?` makes the parameter type `Request | undefined`, which Next.js's route-handler type contract rejects at `next build` time (it requires exactly `NextRequest | Request`). **Failure class B (TypeScript), NOT class A (webpack-alias)** as the Lesson #14 amendment claimed. `tsc --noEmit` does not enforce Next's route-handler contract — only `next build` does — which is why pre-push and the Lint-and-Type-Check CI job both pass clean while the Build Patient App job fails. **Fix applied:** removed the `?` (one character), making `request: Request` non-optional; pattern matches the `DELETE` handler on line 139 of the same file. Only callsite is the re-export in `apps/patient/app/api/patient/sharing/route.ts`. All three tsc gates re-verified clean post-fix (root + per-app patient + per-app clinic — all exit 0). **Outcome:** committed as `80ee270`; CI run 25539467112 is green. **Out-of-scope follow-up surfaced for Mo's ruling AT THE TIME:** the Lesson #14 / ARCH §2 / D-065 amendments in commit `134e272` cite `9774252`'s webpack-alias work as the operational fix for the original CI failure (run 25475031898). That empirical claim is contradicted by the 6-run pattern surfaced this session — the actual fix is the type-handler change applied here. **Resolution:** Mo accepted reframing into a separate cowork session (see "Doc reconciliation + Lesson #17" entry below); commit `80ee270` ships the operational fix, and the doc reconciliation commit ships the empirical-justification correction + Lesson #17. Files touched (this session): `packages/shared/lib/api/handlers/patient/sharing/handler.ts` (1-character source fix), `audits/STATE_OF_WORK.md`.

**Earlier session (kept for context):** Empirical Lesson #14 amendment. CI run 25475031898 (failed with opaque `Process completed with exit code 1`) proved the original two-level path-alias rule (root tsconfig + per-app tsconfig) incomplete — Next 14.2.x's webpack resolver does not always honor tsconfig path aliases for cross-segment imports inside an app. Operational fix shipped in commit `9774252` (per-app webpack alias additions in both apps' `next.config.js`); this session amends the lesson text + cross-refs to reflect three-level reality (root tsconfig + per-app tsconfig + per-app `next.config.js` `config.resolve.alias`). Files touched: `audits/EXECUTION_PROMPTS.md` (Lesson #14 retitled THREE levels, addendum paragraph + rule #2 extended in-place), `ARCHITECTURE.md` §2 (Path alias mechanics paragraph rewritten as 3-level enumeration with CI run + commit `9774252` references), `DECISIONS_LOG.md` D-065 (Amendment 2026-05-07 blockquote subsection added after Outcome — D-004/D-005/D-008 pattern; original Decision body preserved as historical record), `audits/STATE_OF_WORK.md`. ARCH §2 + D-065 amendments are in-scope per Lesson #13 lockstep discipline (half-maintained docs are worse than no docs). **Note:** The empirical claim that `9774252` operationalizes the fix is contradicted by the run 115–120 pattern surfaced in this current session — Lesson #14 may need a second amendment after the actual root cause is identified.

---

## Active workstreams

(One section per workstream currently in flight. Workstream = bounded body of work
with a defined end state. When a workstream is fully done, MOVE its section to
"Completed workstreams" below; do not delete.)

(Phase F Task 18 — Fix `rls_test_teardown()` audit_events cleanup — moved to "Completed workstreams" below; mig 108 applied to staging; matrix re-run at run_no=1.6 = 177/177 PASS; audit_events.S6 stable at 1 per cycle.)

(Phase F Task 10 — auto-renew-on-visit-gpid-lookup admin scope addition — moved to "Completed workstreams" below; admin.ts allow-list extended; bookkeeping fix `d8daa60` → `bad1100` applied; commit shipped to origin/main.)

(Author REVIEW_CRITERIA.md — externalized review framework — moved to "Completed workstreams" below; doc shipped at `audits/REVIEW_CRITERIA.md` (476 lines, 6 sections); Session protocol updated to require reading at every session start; Prompt template subsection added; bookkeeping fix `5ad4003` → `0abce28` applied inline per Path 2 ruling.)

(Dependabot triage + admin scope reconciliation multi-task batch — moved to "Completed workstreams" below; D1 triage of 55 alerts produced `audits/dependabot-triage-2026-05-08.md`; D2 pragmatic root next bump 14.1.0 → 14.2.35 closes 1 critical + 6 high/medium/low at root level; D3 admin-scope reconciliation produced `audits/admin-scope-reconciliation-2026-05-08.md` with Option D recommendation; Phase F Task 16 progressed QUEUED → TRIAGED; new Phase F Tasks 19 + 20 queued.)

(Triage doc empirical correction + Phase F Task 20 multi-task batch — moved to "Completed workstreams" below; Task 1 added §0.5 + §0.6 to triage doc with empirical correction; Task 2 expanded `ALLOWED_ADMIN_SCOPES` 35 → 136 entries + custom eslint rule `medassist-local/no-unregistered-admin-scope` shipped + Lesson #18 codified + REVIEW_CRITERIA §2.1 verification table addition; Phase F Task 20 → DONE; Phase 3 (Option C.1 TypeScript literal-union) remains queued.)

(Phase F closeout autonomous batch — 3 of 4 tasks shipped; Task 3 (TS literal-union refactor) DEFERRED to a focused session per Decision 11 — moved to "Completed workstreams" below. **Tasks DONE:** (1) `.husky/pre-push` extended 3 → 5 passes (added `next build` clinic + patient as Pass 4/5, closing Empirical Lesson #17's operational follow-up); (2) Tier 2 Dependabot — apps' next 14.2.25 → 14.2.35 + apps' eslint-config-next 14.2.25 → 14.2.35 (paired); 21 expected closures (14 apps + 7 lockfile next entries). (4) Phase F Task 19 root next removal audit — categorized as **vestigial template residue**, removal DEFERRED to focused workstream Task 19a (root vestigial dep cleanup, including next + Radix/lucide-react/framer-motion/cmdk hoist-residue triage). **Task DEFERRED:** (3) Phase F Task 16 Phase 3 TS literal-union refactor — defense-in-depth not regressed (yesterday's eslint rule already enforces static-string-only callsites). Sandbox constraint: `next build` exceeds the 45s sandbox timeout for either app, so gate-5 verification is Mac-deferred for this batch (logic verified by sandbox-reproducing the negative claim — gates 1-3 don't catch Lesson #17 anti-pattern, consistent with the lesson's own empirical record). Decision log at `audits/phase-f-closeout-decisions-2026-05-09.md` (12 decisions). **Mac-side cleanup before commit:** `rm -rf apps/clinic/app/api/_lesson17_test` (sandbox rm blocked; file overwritten with benign `export {}` + DELETE comment per the Phase F Task 20 precedent recovery pattern).)

---

## Pending workstreams (queued, not started)

(Highest-priority Phase F follow-ups. Mirror summary; canonical record lives in `audits/PROGRAM_STATE.md` § "Phase F follow-up tasks". Numeric tags below match PROGRAM_STATE numbering. The list is 9 items in PROGRAM_STATE today; planned Tasks 16 (P1) and 17 (P3) are queued in `audits/discipline-cleanup/inventory-2026-05-04.md` but have not yet been added to PROGRAM_STATE.md — surfaced here as "Task 16 (planned)" / "Task 17 (planned)".)

### Workstream: Phase F Task 1 — doctor-fees callsite cleanup + mig 022 archive
- **Why queued:** R1 of forensic plan; mig 022 is dead code on staging behind the global-identity rewrite. Code-side cleanup is decoupled from forensic SQL.
- **Trigger:** Phase D #1.5 matrix reconstruction passes; push to remote releases the Phase F gate.
- **Estimated scope:** s
- **Cross-refs:** `audits/database-audit/mig-022-archive-plan.md`, `audits/database-audit/doctor-fees-usage.md`

### Workstream: Phase F Task 2 — patient_code retirement code deletion
- **Why queued:** R7 of forensic plan; column was never on staging, so DB side is already clean. Codebase still references the column in 4 files.
- **Trigger:** Phase F resumption.
- **Estimated scope:** s
- **Cross-refs:** `audits/database-audit/patient-code-usage.md`; targets `packages/shared/lib/api/handlers/patient/my-code/handler.ts`, `apps/patient/app/api/patient/my-code/route.ts`, `apps/patient/app/(patient)/patient/more/page.tsx` (L359, L394), `packages/shared/lib/data/patients.ts:34`, regenerate `packages/shared/lib/supabase/types.ts`

### Workstream: Phase F Task 3 — `patients.email` exposure decision
- **Why queued:** R4 of forensic plan kept the column. Product decision needed: drop OR wire write path + scope-by-clinic-or-share.
- **Trigger:** Mo product decision.
- **Estimated scope:** s (decision) → m (implementation if "wire write path")
- **Cross-refs:** `doctor/patients/[id]/handler.ts` (returns `patient.email || ''`)

### Workstream: Phase F Task 4 — audit_log vs audit_events consolidation
- **Why queued:** R5 of forensic plan tagged Year-2 tech debt. Two tables coexist with different schemas, writers, consumers — `audit_log` (SMS service + dedup) vs `audit_events` (phone changes pipeline).
- **Trigger:** Year-2 product planning; not blocking launch.
- **Estimated scope:** l
- **Cross-refs:** `audits/database-audit/unclaimed-tables-usage.md`, mig 101 (audit_log backfill)

### Workstream: Phase F Task 5 — `front_desk_staff`-in-policy targeted re-scan
- **Why queued:** Session B's structural-drift spot-check found `invoice_requests::frontdesk_invoice_requests` policy USING-clause was rewritten on staging to use `clinic_memberships` instead of `front_desk_staff`, file body never updated. Pattern is likely systematic across `supabase/migrations/04*.sql`.
- **Trigger:** Phase F resumption (low risk; can run anytime after Phase D #1.5).
- **Estimated scope:** m
- **Cross-refs:** Session B recommendation #2 in `audits/database-audit/structural-drift-spotcheck.md`

### Workstream: Phase F Task 6 — resolve mig 068 status
- **Why queued:** No tracking row; project memory says apply was aborted; superseded by 092-097. Decide: delete from repo, or annotate `.RETIRED` header.
- **Trigger:** Phase F resumption.
- **Estimated scope:** s
- **Cross-refs:** `supabase/migrations/068_cleanup_legacy_policies.sql`

### Workstream: Phase F Task 7 — resolve mig 099 status
- **Why queued:** RPCs target the now-gone `patients.patient_code` column (R7). Dead code.
- **Trigger:** Phase F resumption.
- **Estimated scope:** s
- **Note:** May already be resolved — commit `6adeffa` on 2026-05-04 reads "chore: delete mig 099 (patient_code RPCs) — retired per R7". **Verify before re-doing the work.**
- **Cross-refs:** `supabase/migrations/099_patient_code_rpcs.sql`

### Workstream: Phase F Task 8 — `SET search_path` audit on RLS helpers
- **Why queued:** Defense-in-depth hardening pass before launch. Confirm every `CREATE OR REPLACE FUNCTION` touching RLS-relevant authorization has `SET search_path = public, pg_temp`.
- **Trigger:** Pre-launch hardening (not blocking; queued post-Phase-F-resume).
- **Estimated scope:** s
- **Cross-refs:** mig 092 #1–#4 (helpers), mig 094a `user_has_clinic_path_to_gp`, privacy-code SECURITY DEFINER funcs in mig 087; output is a one-pass enumeration table per `PROGRAM_STATE.md` § Phase F #8

### Workstream: Phase F Task 9 — EXTRA_ON_STAGING reconciliation tooling
- **Why queued:** Tooling pass for `audits/database-audit/AUDIT_FINAL.md` narrative completeness. Confirm policy-count shrinkage from 136 → ~122 EXTRA via migration-tree CREATE POLICY parser + `pg_policies` diff. Same reconciliation for functions (9 → 3) and tables (5 → 0).
- **Trigger:** After Phase F closes; absolute counts (Q1–Q6a per runbook v2 Step 6) already confirm correct state.
- **Estimated scope:** m
- **Cross-refs:** runbook v2 Step 6 outputs; `audits/database-audit/migration-claims-vs-reality.md`

(Phase F Task 16 — admin-scope reconciliation — moved to "Completed workstreams" below as TRIAGED 2026-05-08; recommendation Option D, Phase 2 work queued as Task 20.)

### Workstream: Phase F Task 17 (planned, P3) — en.ts under-population for Build 05 strings
- **Why queued:** Q9 ruling deferred per D-003 Arabic-first; en.ts mirroring is content authoring, not discipline cleanup. Pre-defer verification still required: confirm components don't import en.ts directly.
- **Trigger:** Add to PROGRAM_STATE.md; pre-launch i18n pass.
- **Estimated scope:** s
- **Cross-refs:** `audits/discipline-cleanup/inventory-2026-05-04.md` § "Q9 surfacing"

### Workstream: Phase F Task 19 (planned, P3) — root `next` orphan-dep audit + remove if confirmed
- **Why queued:** Surfaced in 2026-05-08 D2 (Dependabot triage). Root `package.json` declares `"next": "14.1.0"` and four `next *` scripts but has no `app/` / `pages/` / `next.config.js` at the root; apps build via their workspace-local `next@14.2.25`. The pragmatic 14.2.35 bump in this batch's D2 keeps the root `next` temporarily; long-term right answer may be removing it (would close 7 root-only `next` advisories permanently).
- **Trigger:** Post-launch hardening, or sooner if root `next` causes a follow-on issue.
- **Estimated scope:** s–m
- **Cross-refs:** `audits/dependabot-triage-2026-05-08.md` §7.1; D2 commit (this batch); PROGRAM_STATE Task 13.

(Phase F Task 20 — Option A allow-list expansion + mandatory eslint static-string discipline rule — moved to "Completed workstreams" below as DONE 2026-05-09; allow-list expanded 35 → 136; custom rule + plugin + lint:scopes script shipped; Phase 3 (Option C.1) remains the long-term queued work.)

---

## Blocked workstreams (started, then blocked)

None currently — Phase D #1.5 was queued but is now classified as Active above (it has not been started yet, but it is the highest-priority workstream and is unblocked for pickup).

---

## Completed workstreams (chronological)

- **2026-04-30:** Patient identity Builds 02–03 — global_patients + patient_clinic_records foundation; mig 071–082 applied to staging across multiple commits ending with 25/25 + 23/23 validation outcomes.
- **2026-05-01 → 2026-05-02:** Build 04 privacy code feature + sharing data layer foundation. Commit `f61356f` (2026-05-04 UTC by author timestamp; Mo's narrative dates the work 5/1–5/2). mig 083–088 covered.
- **2026-05-02 → 2026-05-03:** Foundation Audit Sessions A / B / C — claims-vs-reality audit, structural drift spot-check, forensic-fix authoring + apply. Migs 100–106 applied to staging. Commit `797a5c3` (foundation audit complete + forensic fixes ready) followed by `23229f8` (forensic apply complete + Phase D queued).
- **2026-05-04:** Build 05 patient sharing lifecycle — initiate / revoke / extend / expire. Commit `61f8752`.
- **2026-05-04:** Per-app TypeScript path aliases — `@patient/*` and `@clinic/*` retire shared `@/*`. Codified by D-065. Commit `bb50305`. Empirical Lesson #14 captures the trap.
- **2026-05-04 (later, by author timestamp = 2026-05-07 UTC):** Phase 5 deep audit reconciliation — ARCHITECTURE.md / DECISIONS_LOG.md / PRODUCT_SPEC.md brought current with shipped reality (Builds 02–05); Empirical Lessons #13 and #16 codified. Commit `19fb148`.
- **2026-05-04 (latest, commit `6934db3`):** Audit corpus artifacts tracked in git — finalizes the audit detour. HEAD as of this STATE_OF_WORK setup.
- **2026-05-06:** Phase D #1.5 matrix reconstruction. **Outcome:** 177/177 PASS at `run_no = 1.5`, fail = 0 across all 24 tables. Push gate to `origin/main` released. **Shipped:** mig 107 (`run_no INTEGER → NUMERIC` + `source_file TEXT` + `rls_test_record(...)` re-creation) + `audits/rls-test-matrix-reconstructed.sql` (executable matrix; per-table DO blocks; tuple loops; SET-LOCAL-ROLE harness pattern). **Comparison vs run #1:** one row diverges, `audit_events.S6` count 8 → 10, attributed to seed-cycle accumulation of `entity_type='patient_clinic_record'` audit events not cleaned by `rls_test_teardown()`; outcomes match (SUCCESS=SUCCESS); NOT an RLS regression. **Architectural ruling:** D-074 (the matrix is the canonical RLS regression artifact for the hybrid 3-DEFINER + 2-INVOKER mode going forward; future RLS-touching changes re-run it before push). **Files changed:** `supabase/migrations/107_alter_rls_test_results_run_no_to_numeric.sql` + `.rollback.sql`, `audits/rls-test-matrix-reconstructed.sql`, `audits/STATE_OF_WORK.md`, `ARCHITECTURE.md` (§8.6 timeline + Database row + §12 regression coverage paragraph), `DECISIONS_LOG.md` (D-074).
- **2026-05-07:** Empirical Lesson #14 amendment — three-level path alias requirement. **Trigger:** CI run 25475031898 failed with opaque `Process completed with exit code 1` because the patient-app build's `@patient/*` imports weren't resolving at the webpack level — Next 14.2.x's webpack resolver does not always honor tsconfig path aliases for cross-segment imports inside an app. **Operational fix already shipped:** commit `9774252` (CI build-log capture + per-app webpack alias additions in both `apps/patient/next.config.js` and `apps/clinic/next.config.js`). **This session amends doc text to reflect three-level reality:** Lesson #14 retitled to "THREE levels" with new "Empirical proof addendum (CI run 25475031898, 2026-05-07)" paragraph + rule #2 extended in-place; ARCH §2 "Path alias mechanics" paragraph rewritten as 3-level enumeration with CI run + commit `9774252` references; D-065 received an `> **Amendment 2026-05-07 — third level required (next.config.js webpack alias)**` blockquote subsection after Outcome (D-004/D-005/D-008 pattern; original two-level Decision body preserved as historical record, amended standing rule supersedes). **Scope decision:** Option (c) per Lesson #13 lockstep discipline — half-maintained docs are worse than no docs. **Files changed:** `audits/EXECUTION_PROMPTS.md`, `ARCHITECTURE.md` §2, `DECISIONS_LOG.md` D-065, `audits/STATE_OF_WORK.md`. **Last commit:** 134e272. **NOTE (added 2026-05-07 follow-up):** The empirical claim in this entry — that `9774252` is the operational fix for the original failure — is contradicted by run 25535878629's build-log artifact (see entry below). Lesson #14 / ARCH §2 / D-065 amendments retain validity as a forward-looking discipline rule (declaring webpack aliases at the third level remains correct best practice), but their cited empirical justification (CI run 25475031898) was probably the same TypeScript route-handler-contract error misdiagnosed as a webpack-alias issue. A second amendment may be warranted; out of scope for the current investigation workstream — flagged for Mo's follow-up ruling.

- **2026-05-07 (later, same day):** CI run 25535878629 root-cause investigation + fix. **Trigger:** Mo reported run 25535878629 failed; same opaque exit-code-1 pattern that prompted commit `9774252`. **Investigation surfaced a broader pattern** — six consecutive failed CI runs (115–120) since commit `19fb148`; last green = `778467a` (run 114, 2026-04-27); every red run failed at the same step. **Root cause** (from build-log artifact uploaded by Mo mid-session): `packages/shared/lib/api/handlers/patient/sharing/handler.ts` line 27 declared `export async function GET(request?: Request)`. The optional `?` makes the parameter type `Request | undefined`, which Next.js's route-handler type contract rejects (it requires exactly `NextRequest | Request`). This was caught by Next at build time, NOT by `tsc --noEmit` (which is why pre-push tsc + the Lint-and-Type-Check job both pass clean — Next's route-handler contract is enforced separately, only at `next build` time). **Failure class B (TypeScript), not Class A (webpack).** **Fix:** removed the `?` — `request: Request` non-optional. Pattern matches the `DELETE` handler on line 139 of the same file. Single-character source change; only callsite is the re-export in `apps/patient/app/api/patient/sharing/route.ts` which preserves the type. **Verification:** all three tsc gates pass clean post-fix (root + per-app patient + per-app clinic); **CI run 25539467112 against `80ee270` is green.** **Mid-session correction to the prior 2026-05-07 entry:** `9774252`'s webpack-alias additions in `next.config.js` were unrelated to the failure mode; the doc amendment in `134e272` (Lesson #14 / ARCH §2 / D-065) is built on a misdiagnosis. **Resolution:** Mo accepted reframing as a follow-on cowork session (see next entry: "CI fix-attribution doc reconciliation + Lesson #17"). **Files changed (this session):** `packages/shared/lib/api/handlers/patient/sharing/handler.ts` (one-character fix), `audits/STATE_OF_WORK.md`. **Last commit:** `80ee270`.

- **2026-05-07 (later still, same day):** CI fix-attribution doc reconciliation + Lesson #17 codification. **Trigger:** the `80ee270` workstream above flagged that the Lesson #14 / ARCH §2 / D-065 amendments shipped in commit `134e272` miscredited `9774252`'s webpack-alias additions as the operational fix for CI run 25475031898 — empirically wrong, since runs 118 (`aa2b991`), 119 (`134e272`), and 120 (`f766a05`) all failed post-`9774252`. **Reframe approach:** annotation, not rewrite — Lesson #14, ARCH §2, and D-065 each receive a sympathetic correction paragraph immediately after the empirically-wrong claim, leaving the existing content intact as a record of what was believed. The path-alias three-level rule itself stands as defensible forward-looking discipline; only the cited empirical justification needed correction. `9774252` is reframed as what it actually was: (a) a build-log-capture infrastructure commit that enabled diagnosis six runs later — load-bearing operational discipline, and (b) preventive third-declaration-site webpack aliases — defensible discipline in their own right but not the active bug fix. **Lesson #17 codified** in `audits/EXECUTION_PROMPTS.md` between Lesson #16 and the prompt-tracker block: title is "`tsc --noEmit` does not enforce Next.js route-handler type contracts." Standing rule: route handlers and the handlers they re-export must accept their first arg non-optionally as `request: Request | NextRequest`. Reviewers reject any `?` on the first parameter. Body logic that conditionally uses `request` narrows inside the function. Phase F follow-up flagged (out of scope): consider adding `next build` to the pre-push gate or as a fast-fail CI step. **Files changed (this session):** `audits/EXECUTION_PROMPTS.md` (Lesson #14 receives "Correction 2026-05-07 (later, post-investigation)" paragraph + new Lesson #17 added after Lesson #16), `ARCHITECTURE.md` §2 (correction blockquote appended), `DECISIONS_LOG.md` D-065 (second amendment blockquote — "Amendment 2026-05-07 (later) — empirical-justification correction"; footer updated), `audits/STATE_OF_WORK.md` (this entry, header, methodology rules section). **Last commit:** `bad1100`. *(Bookkeeping fix 2026-05-08: this entry originally recorded the hash as `d8daa60`, which was incorrect — verified against `git log` that the actual commit hash on origin/main for "docs: reconcile CI fix attribution; codify Lesson #17 (tsc vs next build)" is `bad1100`. Corrected as part of Phase F Task 10's commit.)*
- **2026-05-07:** Phase F Task 18 — Fix `rls_test_teardown()` audit_events cleanup. **Outcome:** mig 108 applied to staging; matrix re-run at `run_no = 1.6` produces **177/177 PASS, fail=0 across all 24 tables**, `audit_events.S6` row count stable at 1 per cycle (was 8 in run #1, 10 in run #1.5 due to the unfixed teardown). **Shipped:** mig 108 (forward + rollback) extending `audit_events` DELETE clause with `resolved_global_patient_id = ANY(test_gps)` + defensive `entity_type='patient_clinic_record' AND metadata->>'global_patient_id' = ANY(test_gps)` (the latter is functionally redundant since `resolved_global_patient_id` is a GENERATED ALWAYS column, but kept for readability). **Investigation findings:** (a) live `rls_test_teardown()` body on staging is a SUPERSET of `audits/rls-test-seed.sql` (extra ops/comm + clinical-data cleanup blocks added in Sessions 11 + 14a — the seed.sql file is out of date relative to staging, Lesson #7 in action; mig 108 redefines authoritatively from the live body); (b) `audit_events.resolved_global_patient_id` is a GENERATED ALWAYS column (computed from `metadata->>'global_patient_id'` or `entity_id` when `entity_type='global_patients'`), not a separate resolver-trigger output — so smoke probe omits it from the INSERT and lets the generated expression compute it; (c) only one audit-emitting trigger surfaced on seeded tables (`tg_audit_pcr_insert_trg` on `patient_clinic_records`, mig 088) — fix was bounded to a single audit_events DELETE clause extension. **Files changed:** `supabase/migrations/108_fix_rls_test_teardown_audit_events.sql` + `.rollback.sql`, `audits/rls-test-matrix-reconstructed.sql` (run_no 1.5→1.6 in 24 DO blocks + Section 0; audit_events.S6 dynamic→hardcoded 1; Section 25 validation queries updated; new D-074 amendment commentary in Section 19), `ARCHITECTURE.md` (§3 row count 110→111, highest base 107→108; §8.6 mig 108 row), `DECISIONS_LOG.md` (D-074 amendment blockquote), `audits/STATE_OF_WORK.md`, `audits/PROGRAM_STATE.md` (Task 18 marked Done).

- **2026-05-08 (later, same day):** Author `audits/REVIEW_CRITERIA.md` — externalized review framework. **Trigger:** today's pattern of "fresh cowork session per workstream → surface → Mo reviews → apply → commit → push" works but each fresh session re-loads ~10K tokens of context before doing useful work. Externalizing the review criteria into a versioned doc lets future cowork sessions self-check deliverables before surfacing, surface fewer false-positives, and operate in longer continuous sessions without losing quality. Mo is no longer the sole holder of review criteria; the doc joins the lockstep set so the framework stays in sync as the methodology evolves. **Doc shape:** 476 lines, 6 sections (Pre-work verification / Mid-work checkpoints / Surface format / Failure-mode catalog / Push-back authorization / Maintenance). §4 catalogs 6 failure-mode patterns each backed by concrete examples from this project's history (5 from `doc-verification-sweep-2026-05-04.md` — enum drift, phantom claims, count-shorthand drift, forward references without flag, off-by-amend hash artifacts; 1 from session history — misattribution of cause). Empirical grounding is non-negotiable; abstract criteria without project examples were intentionally cut. **Pre-work verification gate is empirically demonstrated:** while authoring this very doc, §1.2's STATE_OF_WORK currency check caught an unreachable-orphan hash in line 125 (`5ad4003` was orphaned by `git commit --amend`; actual HEAD is `0abce28`). Per Path 2 ruling (precedent: Phase F Task 10 bundled the `d8daa60 → bad1100` fix inline), the bookkeeping correction shipped in the same commit that codified the discipline. Annotation pattern preserves the original incorrect hash for audit trail rather than silently overwriting. **Sympathetic doc updates:** STATE_OF_WORK.md "Session protocol" now requires reading REVIEW_CRITERIA.md at every session start; new "Prompt template" subsection notes every workstream prompt cites §1.1's pre-work verification gate; methodology rules in force adds REVIEW_CRITERIA.md as a lockstep peer; ARCH §16 preamble notes the new lockstep doc; EXECUTION_PROMPTS.md "Empirical lessons" preamble points to REVIEW_CRITERIA.md as the operational distillation of Lessons #1-#17 into a checklist cowork sessions can run mechanically. **Files changed:** `audits/REVIEW_CRITERIA.md` (new, 476 lines), `audits/STATE_OF_WORK.md` (header + Session protocol + Methodology rules + workstream entry + bookkeeping fix `5ad4003 → 0abce28`), `audits/EXECUTION_PROMPTS.md` (Empirical lessons preamble), `ARCHITECTURE.md` (§16 lockstep-set note appended). Closes the protocol-evolution workstream queued after Phase F Task 10. Pairs with Lesson #16 (verify against ground truth) and Lesson #13 (lockstep maintenance).

- **2026-05-09:** Triage doc empirical correction + Phase F Task 20 multi-task batch (Task 1 + Task 2). **Trigger:** yesterday's batch left two follow-on threads — (1) my D2 surface said "predicted 7, actual 5; 2 stragglers were 5j59 + mwv6"; today's empirical pull (`audits/dependabot-alerts-2026-05-09.json` + `audits/ci-runs-2026-05-09.json`) revealed exactly 7 closures fired at `2026-05-09T03:15:29Z`; the framing error was net-delta-vs-closure-count conflation. (2) Phase F Task 20 (Phase 2 of yesterday's Option D plan) was queued for "next workstream" pickup. **Pre-work verification:** HEAD = `39c2f43` ✓; CI run 25590181686 = success ✓; alert state distribution 50 open / 25 fixed / 5 auto_dismissed = 80 total ✓; D1's predicted-7 closures all in fixed state with `fixed_at = 2026-05-09T03:15:29Z` ✓; manifest-double-counting hypothesis empirically confirmed for 5j59 + mwv6 (each has 4 alert rows, with one nuance — the root `package.json` entries closed on 2026-05-07 by a Dependabot tracking-model migration unrelated to our work) ✓; admin-scope inventory unchanged (135 / 35 / 105 / 5 / 30) ✓. **Task 1 outcome:** added §0.5 (Empirical correction) + §0.6 (Lesson #18 candidate) to `audits/dependabot-triage-2026-05-08.md`. Doc grew 379 → 488 lines (109 new). Original prediction text in §6 untouched per Lesson #14 amendment pattern. Section 0.5 documents (a) D1's prediction held 100%, (b) net delta arithmetic in two baselines (54 push-output → 7 closures + 3 arrivals; 55 yesterday-snapshot → 7 closures + 2 arrivals; both arrive at 50), (c) manifest-double-counting empirically confirmed with the 2026-05-07 Dependabot housekeeping nuance, (d) honest record of my D2-surface framing error. Section 0.6 codifies Lesson #18 as a candidate. **Task 2 outcome:** `ALLOWED_ADMIN_SCOPES` expanded 35 → 136 entries (105 added in 16 feature blocks; 4 truly-unused removed; the "132" target in yesterday's reconciliation doc was a math error — correct arithmetic 35 + 105 − 4 = 136, captured as §9.1 of the reconciliation doc). Custom eslint rule `eslint-rules/no-unregistered-admin-scope.js` shipped (rule logic + `eslint-rules/index.js` plugin packaging + `eslint-rules/package.json` `file:` linkage + root `package.json` devDependencies update + `.eslintrc.json` plugin/rule wiring + new `lint:scopes` root script for scanning packages/shared/lib + apps/{clinic,patient}/app with a minimal eslint config). Three rule classes empirically validated by smoke tests against contrived bad commits (unregistered scope, template literal, variable arg — all fire correctly). **Five verification gates clean:** `lint:scopes`, three tsc gates, per-app `next lint` clinic + patient. The two `next build` gates run on Mo's Mac per Lesson #17. **Lockstep doc updates landed:** ARCH §12 (count update + Phase 2 narrative), DECISIONS_LOG D-008 Amendment 2026-05-09 appended (third amendment — Phase 2 mechanism + math correction), reconciliation doc §9 appended (Phase 2 shipped + math correction), `audits/EXECUTION_PROMPTS.md` Lesson #18 codified (filter by event timestamp, not net delta), `audits/REVIEW_CRITERIA.md` §2.1 verification-table closure-counts row pointing at Lesson #18, PROGRAM_STATE Task 20 → DONE, this STATE_OF_WORK header + Last session + workstream lifecycle. **Out-of-scope finding (sandbox-permission limitation):** smoke test left a scratch file at `apps/clinic/app/__rule-test-bad-scope.tsx` (sandbox `rm` blocked by mount permissions); content replaced with a benign `export {}` stub flagged "DELETE BEFORE COMMIT". Mo deletes before pushing the batch commit. **Files changed:** `audits/dependabot-triage-2026-05-08.md` (+109 lines: §0.5 + §0.6), `audits/dependabot-alerts-2026-05-09.json` (new — raw evidence per yesterday's pattern), `audits/ci-runs-2026-05-09.json` (new), `audits/admin-scope-reconciliation-2026-05-08.md` (+§9 Phase 2 shipped section), `audits/EXECUTION_PROMPTS.md` (Lesson #18 added between #17 and prompt-tracker block), `audits/REVIEW_CRITERIA.md` §2.1 (closure-counts row), `packages/shared/lib/supabase/admin.ts` (allow-list expansion), `eslint-rules/no-unregistered-admin-scope.js` (new), `eslint-rules/index.js` (new), `eslint-rules/package.json` (new), `package.json` (devDependencies + lint:scopes script), `package-lock.json` (file: linkage), `.eslintrc.json` (plugin + rule), `ARCHITECTURE.md` §12, `DECISIONS_LOG.md` D-008, `audits/PROGRAM_STATE.md` Task 20 → DONE, `audits/STATE_OF_WORK.md`. **Last commit:** TBD (this batch). Closes Phase F Task 20 (P1). Phase 3 (Option C.1 TypeScript literal-union refactor) remains queued.

- **2026-05-08 (later, same day — final session of the day):** Dependabot triage + admin-scope reconciliation multi-task batch (D1+D2+D3). **Trigger:** pre-launch security hardening (queued in Open Strategic Decisions) + Phase F Task 16 admin-scope reconciliation (queued in Pending workstreams). Bundled because both surface from D-008's "audit-friendly admin access" claim being structurally correct but operationally weakened, and produce paired audit artifacts. **Pre-work verification (REVIEW_CRITERIA §1.1):** HEAD = `4177524` ✓; CI run 25585424992 (success) at HEAD ✓; alert count drift surfaced (54 → 55, +1 high `fast-uri ≤ 3.1.0` created during the session itself, prompt's "surface and continue" rule held); `audits/REVIEW_CRITERIA.md` exists at 476 lines ✓; lockstep peers coherent. **D1 outcome:** triage of 55 open alerts (29 unique GHSAs after manifest dedup); 1 critical / 22 high / 25 medium / 7 low; remediation classes Tier 1 (8 GHSAs, version-bump fix), Tier 3 (5 GHSAs, Next 15 major bump deferred), Tier 4 (16 GHSAs theoretical-build-pipeline). **D2 outcome:** pragmatic root next bump 14.1.0 → 14.2.35; lockfile delta 113 lines (54/61 on Mac canonical install) bounded entirely to next dep tree; apps' next 14.2.25 untouched. Closes critical CVE-2025-29927 + 6 root-only `next` advisories at zero extra blast radius within 14.2.x. Three tsc gates clean (sandbox + Mac local) + both apps' `next build` passed locally — Lesson #17 gap closed empirically (route-handler contracts validated via real build, not just tsc). `npm audit --json` post-bump: critical 1 → 0 ✓. **D3 outcome:** admin-scope inventory complete; 210 total `createAdminClient(...)` invocations (207 explicit-arg + 3 no-arg); 135 unique callsite scopes vs 35 allow-list (precise; supersedes "~36" approximation); 105 missing; 4 truly-unused; 0 typos / near-misses; 0 dynamic template-literal callsites; uniform kebab-case naming; 100% production code-path. Largest cluster: Build 05 patient-sharing lifecycle (D-068), 18 of the 105. Recommended **Option D (sequenced hybrid)**: Phase 1 inventory only (this batch); Phase 2 (Task 20) Option A allow-list expansion (35 → 132) + MANDATORY eslint static-string discipline rule (without rule, expansion is cosmetic); Phase 3 (m) Option C.1 TypeScript literal-union refactor; Option B runtime-throw likely DROPPED. **Out-of-scope rulings applied this session:** ARCH §12 count fix (~36 → 35) per Lesson #13 lockstep; 4 truly-unused entries deferred to Phase 2 (don't fragment the survey); 0-dynamic-templates precondition captured for Phase 2 eslint rule (locks forward, not just snapshot); root `next` orphan-dep audit decoupled as Phase F Task 19 (P3); JSON dumps committed as raw evidence per pattern of `staging-schema-2026-05-03.sql`; `/tmp/*.tsv` working files NOT committed (codebase state is git-versioned). **Doc updates landed:** ARCH §12 admin-scope count + reconciliation pointer; DECISIONS_LOG D-008 dual amendments (Phase F Task 10 hash bookkeeping fix `5ad4003` → `0abce28` per Path 2 annotation pattern + Amendment 2026-05-08 capturing inventory + Option D); PROGRAM_STATE new Tasks 12 (Task 16 TRIAGED), 13 (Task 19 P3 root next removal), 14 (Task 20 P1 Option A + eslint); STATE_OF_WORK header + workstream lifecycle + Open-strategic-decisions update + summary-list refresh. **Files changed:** `audits/dependabot-triage-2026-05-08.md` (new, 379 lines), `audits/dependabot-alerts-2026-05-08.json` (new), `audits/ci-runs-2026-05-08.json` (new), `audits/admin-scope-reconciliation-2026-05-08.md` (new, ~370 lines after Mo's three additions), `package.json` (next 14.1.0 → 14.2.35), `package-lock.json` (113-line bounded delta), `ARCHITECTURE.md` §12, `DECISIONS_LOG.md` D-008, `audits/PROGRAM_STATE.md` Phase F task list, `audits/STATE_OF_WORK.md`. **Last commit:** TBD (this batch). Closes triage portion of D-008 Amendment 2026-05-04 follow-up. Phase F Task 16 progresses QUEUED → TRIAGED.

- **2026-05-08:** Phase F Task 10 (P0) — `auto-renew-on-visit-gpid-lookup` admin scope addition to `ALLOWED_ADMIN_SCOPES`. **Trigger:** the Build 05 § B7 / D-068 auto-renew-on-visit hook in the frontdesk check-in handler calls `createAdminClient('auto-renew-on-visit-gpid-lookup')` (`packages/shared/lib/api/handlers/frontdesk/checkin/handler.ts:145`) — a scope that wasn't in the allow-list, producing a `console.warn` at runtime on every active-share check-in event. **Verification (Lesson #7):** read the call-site source directly; scope string matched the task description verbatim; `ALLOWED_ADMIN_SCOPES` confirmed not to already contain it or any kebab/snake/camel near-duplicate. **Fix:** single-line addition under a new comment-grouped block (`// ── Patient sharing lifecycle (mig 090 / Build 05 § B7 — D-068) ──`), positioned after the existing "Global patient identity" group; kebab-case convention preserved. **Three tsc gates clean** (`npm run type-check` root + `-w @medassist/clinic` + `-w @medassist/patient`). **Out-of-scope finding surfaced for Task 16 (planned, P1):** `packages/shared/lib/data/patient-shares.ts:332` uses `createAdminClient('patient-shares-auto-renew')` — also missing from the allow-list, but a different logical scope (the share renewal RPC inside `autoRenewOnVisit`, not the GPID lookup at the handler call site); not a near-duplicate of the same logical scope, so the workstream's "ONE specific scope" boundary holds. Recorded in PROGRAM_STATE Task 10 closure note for the broader Task 16 work to absorb. **Bookkeeping fix in same commit:** STATE_OF_WORK's prior CI doc-reconciliation entry recorded the commit hash as `d8daa60`, which was incorrect — `git log` shows the actual hash for "docs: reconcile CI fix attribution; codify Lesson #17 (tsc vs next build)" is `bad1100`. Corrected with an inline annotation. **Files changed:** `packages/shared/lib/supabase/admin.ts` (one-line + comment-header addition), `ARCHITECTURE.md` §12 (~35 → ~36 in allow-list), `DECISIONS_LOG.md` D-008 Amendment 2026-05-04 (one-sentence closure note appended), `audits/PROGRAM_STATE.md` (new Task 10 entry as ✅ DONE), `audits/STATE_OF_WORK.md` (header + workstream Active→Completed move + bookkeeping fix). **Last commit:** `0abce28` (originally recorded as `5ad4003` — off-by-amend artifact, corrected during REVIEW_CRITERIA.md authoring 2026-05-08 per the new pre-work verification gate; identical pattern to line 122's `d8daa60 → bad1100` fix; verified against `git log` ground truth that `0abce28` is the actual hash on origin/main for "chore(admin-scope): add auto-renew-on-visit scope to ALLOWED_ADMIN_SCOPES (P0)" with Author timestamp `Fri May 8 14:55:52 2026 -0700`). Broader admin-scope reconciliation (Phase F follow-up Task 16/18+) remains queued.

---

## Phase F follow-up tasks (operational backlog)

(Mirror of `audits/PROGRAM_STATE.md` Phase F task list — SUMMARY only; PROGRAM_STATE is the canonical record. Rebuild this section from PROGRAM_STATE at every session start. Currently 9 canonical items + 4 done (Task 18, Task 10, Task 16 TRIAGED, Task 20 DONE) + 1 queued P3 (Task 19) + 1 planned-not-yet-canonical (Task 17); see "Pending workstreams" above for one-liners + triggers.)

1. Doctor-fees callsite cleanup + mig 022 archive (R1) — **s**
2. patient_code retirement code deletion (R7) — **s**
3. `patients.email` exposure decision (R4) — **s decision / m impl**
4. `audit_log` vs `audit_events` consolidation (R5) — **l**, Year-2 tech debt
5. `front_desk_staff`-in-policy targeted re-scan (Session B rec #2) — **m**
6. Resolve mig 068 status — **s**
7. Resolve mig 099 status — **s** (likely already done in commit `6adeffa`; verify)
8. `SET search_path` audit on RLS helpers — **s**, pre-launch hardening
9. EXTRA_ON_STAGING reconciliation tooling — **m**, post-Phase-F
10. Task 18 — Fix `rls_test_teardown()` audit_events cleanup — **✅ DONE 2026-05-07** (mig 108; matrix re-run at run_no=1.6 = 177/177 PASS; audit_events.S6 stable at 1)
11. Task 10 — `auto-renew-on-visit-gpid-lookup` admin scope addition — **✅ DONE 2026-05-08** (admin.ts allow-list extended; runtime `console.warn` eliminated for the active-share check-in path; broader Task 16 reconciliation TRIAGED below)
12. Task 16 — admin-scope reconciliation — **✅ TRIAGED 2026-05-08** (135 unique callsite scopes / 35 allow-list / 105 missing / 0 typos / 0 dynamic; recommended Option D sequenced hybrid; Phase 2 work shipped as Task 20)
13. Task 19 — root `next` orphan-dep audit + remove if confirmed (P3) — **s–m**, post-launch hardening
14. Task 20 — Phase 2 of admin-scope reconciliation: Option A allow-list expansion (35 → 136) + mandatory eslint static-string discipline rule — **✅ DONE 2026-05-09** (allow-list expanded; custom rule `medassist-local/no-unregistered-admin-scope` shipped + smoke-tested against three error classes; 5 verification gates clean; runtime `console.warn` retained as defense-in-depth during Phase 2 → Phase 3 transition; Phase 3 (Option C.1 TypeScript literal-union) remains queued)
- **Task 17 (planned, P3):** en.ts under-population for Build 05 strings — **s**, pre-launch i18n

---

## Open strategic decisions

(Decisions that need to be made but aren't time-sensitive. Promoted to active workstreams when a session takes them on.)

- **WhatsApp delivery:** PRODUCT_SPEC mentions WhatsApp 6 times; today implementation is Twilio SMS only with a `sendWhatsApp()` stub at `packages/shared/lib/sms/twilio-client.ts:60-62`. Decide: update code (integrate 360dialog/WATI) or update spec (SMS-first acceptable for v1). Source: `audits/01-feature-audit.md` C3.
- **Drug interaction rules:** PRODUCT_SPEC promises ~500 critical drug interactions as a P0 hook. Current state per `audits/01-feature-audit.md` is "dramatically thinner than PRODUCT_SPEC describes." Decide: build internally, license a database, or partner.
- **E-Fatorah / Egyptian Tax Authority e-receipt integration:** Scope and timing not yet decided. No callsite audit performed; surfaced here for tracking.
- ~~**Dependabot vulnerabilities (1 critical + 41 others):** Surfaced in the 2026-05-04 push (post-`accc580` package-lock restore). Triage and remediation plan needed before launch hardening.~~ **TRIAGED + critical remediated 2026-05-08:** triage doc at `audits/dependabot-triage-2026-05-08.md` (55 alerts, 29 unique GHSAs); critical CVE-2025-29927 closed via root next 14.1.0 → 14.2.35; remaining alerts categorized into Tier 2 (apps bump to 14.2.35), Tier 3 (Next 15 major bump — separate workstream), Tier 4 (build-pipeline transitives — quarterly dep-update sprint).

---

## Methodology rules in force

(Compressed reference list of Empirical Lessons currently active. Cross-references `audits/EXECUTION_PROMPTS.md` § "Empirical lessons" for the full body.)

- **Lesson #1 (amended 2026-04-30 → 2026-05-03):** RLS helpers default to SECURITY DEFINER; INVOKER allowed only when internal queries provably do NOT trigger RLS recursion through the helper itself. Current locked set: 3 DEFINER + 2 INVOKER hybrid (`is_clinic_member` / `can_view_patient_data_at_clinic` / `user_has_clinic_path_to_gp` DEFINER; `can_clinic_access_global_patient` / `can_patient_access_global_patient` INVOKER post-mig-106). See D-064.
- **Lesson #2:** Smoke-probe assertion in every RLS migration — `SET LOCAL ROLE 'authenticated'` + a real SELECT/INSERT inside the migration's transaction. Structure-only assertions cannot catch query-time recursion.
- **Lesson #3:** Test harness pattern — `SET LOCAL ROLE 'authenticated'` as a separate statement, NOT inside a WITH clause's `set_config(...)`. Inline form leaves the plan built under session_user's BYPASSRLS attribute.
- **Lesson #4:** Prototype-before-author for any RLS work touching cross-table predicates. A 10-minute harness probe is cheaper than authoring scenarios against a recursion-broken policy.
- **Lesson #5:** Cross-clinic write-asymmetry probe technique — patient with cross-clinic PCR visibility can be used to bypass write triggers and prove RESTRICTIVE INSERT policies hold in isolation.
- **Lesson #6 (Phase F session 16, 2026-05-02):** Per-callsite re-reading at Phase F start. Triage tags are sorting hints, not decisions. Re-read each callsite to confirm what it does AND whether it predates relevant locked decisions before authoring RPC / MIGRATE-TO-USER / KEEP-ADMIN.
- **Lesson #7 (Foundation Audit Session A, 2026-05-03):** Verify schema state independently of the migration tree. Schema is ground truth; migration files are intent. When they disagree, staging wins by default until a decision is made.
- **Lesson #8 (Sessions A + C, 2026-05-03):** All schema changes go through committed migration files. No dashboard SQL editor applies. No `supabase db push` of uncommitted SQL. No `supabase migration repair --status applied` without a corresponding committed file.
- **Lesson #9 (Session C apply phase, 2026-05-03):** Audit the temporal sequence of dashboard SQL applies, not just current state. Before re-running recovered SQL, enumerate every column / function / table it references and confirm shape against current `information_schema` / `pg_catalog`. Out-of-band drift gets logged in `audits/database-audit/out-of-band-post-2026-04-08.md`.
- **Lesson #10 (Session C apply phase, 2026-05-03):** Use programmatic enumeration for caller surfaces, not manual audit-doc reading. The verification SQL belongs in the verification doc verbatim, with row count.
- **Lesson #11 (Session C apply phase, 2026-05-03):** MCP `apply_migration` and CLI `supabase migration up` have different tracking-row semantics. CLI skips already-tracked versions silently; MCP always creates a new tracking row. Pick the right tool for the goal — alignment-without-state-change with a committed file warrants neither.
- **Lesson #12 (Session C + Phase D #1.5 pre-flight, 2026-05-03):** Test scaffolding must be persisted as durable executable code, not ephemeral interactive SQL. `_test_results` outcomes without committed source SQL are a regression-baseline anti-pattern. Add `source_file TEXT` to outcomes tables.
- **Lesson #13 (Doc reconciliation pass, 2026-05-04):** Source-of-truth documentation must be maintained in lockstep with the code and schema it describes. Half-maintained docs are worse than no docs. Every architecturally-significant change requires a corresponding doc update in the same commit (or a paired same-session commit). ARCHITECTURE / DECISIONS_LOG / PRODUCT_SPEC are tracked in git from day one.
- **Lesson #14 (Discipline cleanup, 2026-05-04 — amended 2026-05-07):** Per-app TypeScript path aliases use a per-app prefix (`@patient/*`, `@clinic/*`) and are declared at all three levels: root `tsconfig.json` (pre-push tsc gate), per-app `tsconfig.json` (Next.js dev/build), and per-app `next.config.js` `config.resolve.alias` (webpack at build time — Next 14.2.x's resolver doesn't always honor tsconfig paths for cross-segment imports inside an app, surfaced by CI run 25475031898; operational fix in commit `9774252`). Shared `@/*` across apps is BANNED. Codified by D-065 (with 2026-05-07 amendment subsection).
- **Lesson #15 (reserved):** Empty slot — preserved so ARCHITECTURE.md / DECISIONS_LOG.md forward references remain valid. Reassign or delete when the next lesson is authored.
- **Lesson #16 (Doc verification sweep, 2026-05-04):** Documentation claims about the system require verification against the system, not against project memory or prior session drafts. Every doc-touching cowork session ends with a verification sweep that produces a report-shaped artifact (see `doc-verification-sweep-2026-05-04.md`). Constants, counts, names, paths, security parameters, hashes, dates — all verified against ground truth.
- **Lesson #17 (CI fix-attribution reconciliation, 2026-05-07):** `tsc --noEmit` does not enforce Next.js route-handler type contracts; only `next build` does. Route handlers under `app/api/**/route.ts` and the handlers they re-export from `packages/shared/lib/api/handlers/**` must accept their first parameter non-optionally as `request: Request` or `request: NextRequest` — never `request?: Request` (the `?` makes the type `Request | undefined`, which Next's contract rejects at build time). Reviewers reject any `?` on a route handler's first param; body logic that conditionally uses `request` narrows inside the function. Empirical proof: commits `61f8752` → `80ee270` bracket six consecutive failed CI Build Patient App runs (115–120) where pre-push tsc + Lint-and-Type-Check both passed clean. Phase F follow-up flagged: consider `next build` in the pre-push gate or as a fast-fail CI step.
- **Review framework (REVIEW_CRITERIA.md, 2026-05-08):** `audits/REVIEW_CRITERIA.md` is the operational checklist of review checks the cowork session applies to its own deliverables before surfacing. Six sections: pre-work verification (claims / STATE_OF_WORK currency / cross-reference resolvability), mid-work checkpoints (verification before claim, lockstep trigger list, continuation rule), surface format requirements (required elements, conventions, what NOT to include), failure-mode catalog (6 empirically-grounded patterns), push-back authorization (when to push back on Mo's prompts), and maintenance (lockstep peer triggers). Joins the lockstep set: ARCH / DECISIONS_LOG / PRODUCT_SPEC / STATE_OF_WORK / EXECUTION_PROMPTS / REVIEW_CRITERIA all update together when the methodology evolves. Required reading at every cowork session start.

---

## Session protocol (load every session start)

1. Read this `STATE_OF_WORK.md` end-to-end before any work AND read `audits/REVIEW_CRITERIA.md` end-to-end before any work. Both are required reading; the second is the operational checklist of review checks the session applies to its own deliverables before surfacing.
2. Run `audits/REVIEW_CRITERIA.md` §1 pre-work verification against the prompt — confirm empirical claims hold against ground truth, STATE_OF_WORK.md is current with HEAD, and cross-references resolve. Surface contradictions BEFORE authoring.
3. Pick up the active workstream's Next Action OR start the highest-priority pending workstream.
4. Update `STATE_OF_WORK.md` "Currently doing" before starting work.
5. At session end (whether work is done or paused):
   - Update active workstream "Status", "Currently doing", "Next action", "Last commit".
   - Update "Last updated" + "Last session" header.
   - Update the 3 core docs (`ARCHITECTURE.md`, `DECISIONS_LOG.md`, `PRODUCT_SPEC.md`) for any architectural / decision / product-scope changes shipped this session — even if a single sentence.
   - Update `audits/REVIEW_CRITERIA.md` if a new failure-mode pattern surfaced this session OR Mo's feedback contradicted an existing criterion. The doc is part of the lockstep set.
   - Commit `STATE_OF_WORK.md` + the 3 core docs (+ REVIEW_CRITERIA.md if changed) together.
6. Surface session-end report referencing `STATE_OF_WORK.md`, not project memory. Apply REVIEW_CRITERIA.md §3 surface format requirements — required elements, conventions, what NOT to include.

### Prompt template (standing requirement)

Every workstream prompt henceforth includes a "Pre-work verification" reference: cowork session verifies the prompt's empirical claims against ground truth BEFORE starting work, per `audits/REVIEW_CRITERIA.md` §1.1. Contradictions surface as a STOP (never silent execution). The prompt is input data, not infallible spec — REVIEW_CRITERIA.md §5 grants the cowork session explicit authority to push back on Mo's prompts when premises are wrong, scope is internally inconsistent, or the verification methodology won't actually verify what it claims to verify.
