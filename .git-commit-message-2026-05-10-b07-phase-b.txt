feat(b07): Phase B schema migrations — Dependent Accounts (Pattern A + Pattern B)

B07 Phase B per the architectural review (commit 07fcbf8) plus Mo's three
review additions on the first surface (2026-05-10): (1) mig 112
grantor≠delegate CHECK + mig 110 smoke-probe single-user graceful skip;
(2) Empirical Lesson #19 codified; (3) explicit post-Phase-B follow-up
tracker entries in PROGRAM_STATE.md.

Schema-only; no data-layer / RLS / API / UI changes in this phase. Mo
review at end of phase before Phase C-E batch.

Mo's 17 load-bearing rulings preserved (10 architecture + 4 cowork-design +
3 protocol). Decision log at audits/b07-phase-b-execution-2026-05-10.md
captures all 13 phase-internal trade-offs (11 from initial Phase B + 2
from the review-additions batch).

Pre-work fired STOP exceptions #1 and #4 — Mo resolved before any DDL:
  - STOP #1: live global_patients.normalized_phone is already nullable per
    mig 076 §076.0 (deliberate sentinel/quarantine pattern), contradicting
    the architectural review's NOT NULL claim.
  - STOP #4: dep #1's parent_phone phone-lookup self-resolves to the
    child's own gp because the child gp's normalized_phone IS the parent's
    phone (old data convention). No separate parent gp existed.
  - Mo rulings: Q1 Option D investigate→Option B drop "must have phone"
    CHECK; Q2 Option A reconstruct + relink; Q3 proceed within authority.

Migrations shipped (4):

1. mig 109 — global_patients minor-gp schema additions
   - guardian_global_patient_id UUID FK ON DELETE SET NULL + is_minor
     BOOLEAN NOT NULL DEFAULT FALSE + CHECK minor_requires_guardian +
     CHECK minor_no_self_claim + partial index on guardian fk WHERE NOT
     NULL.
   - "Non-minor must have phone" CHECK deliberately dropped (Decision 1)
     because mig 076 already relaxed normalized_phone for sentinels.
   - Smoke probe POS + 3 NEG cases all PASS.

2. mig 110 — patient_delegations table (Pattern B)
   - 17 columns, 2 CHECKs (delegate_not_self, revoke_consistency),
     partial unique index for active uniqueness (Decision 2 — partial
     unique index fallback because btree_gist not enabled on staging),
     2 operational indexes, updated_at trigger.
   - Capability tokens NOT validated at DB (D-008 eslint-locked literal-
     union pattern at app layer in Phase E). MVP capability set is the 5
     from B07 rulings; consent_to_share is post-MVP.
   - Smoke probe POS + 3 NEG + updated_at trigger all PASS (Decision 8 —
     iteration: NOW() returns transaction-start time within a single tx,
     so seeded updated_at='2000-01-01' at INSERT to make trigger
     advancement observable). Single-user fallback updated to graceful
     skip per Decision 12 (mig 112 makes the previous fallback path self-
     violate the new grantor≠delegate CHECK).

3. mig 111 — Backfill 3 legacy dependents
   - 3 child gps flipped to is_minor=TRUE with guardian_global_patient_id
     resolved via FK (dep #3), phone (dep #2), or 3-step reconstruction
     (dep #1 — Decision 9). New parent gp a108411a-…- created for dep #1
     (فاطمة أحمد) with placeholder display_name='ولي أمر فاطمة أحمد';
     correctable by clinic frontdesk later.
   - 4 audit_events emitted with actor_kind='migration'. Decision 10
     surfaces non-obvious schema fact: audit_events.resolved_global_patient_id
     is GENERATED ALWAYS (not insertable directly).
   - Legacy patients rows preserved (cleanup deferred to Prompt 6.5).

4. mig 112 — patient_delegations grantor ≠ delegate CHECK (NEW from Mo's
   review-additions batch, Decision 12)
   - Single CHECK: granted_by_user_id <> delegate_user_id.
   - Closes a schema-design gap caught on review of the first Phase B
     surface — review §5.3's two-step "principal grants; delegate accepts"
     presumes grantor and delegate are different humans. Mig 110 enforced
     delegate ≠ principal at the gp level but did not enforce
     grantor ≠ delegate at the user level.
   - Smoke probe POS (distinct grantor + delegate) + NEG (grantor =
     delegate, expect check_violation) both PASS.
   - The originally-conditional mig 112 (mig 081 compat-trigger amendment)
     was SKIPPED per Decision 3 — investigation revealed mig 081 operates
     only on clinical tables (clinical_notes, prescription_items,
     appointments, lab_orders, imaging_orders, vital_signs,
     patient_consent_grants, doctor_patient_relationships,
     patient_visibility, lab_results, patient_phone_history); zero
     is_dependent / is_minor / phone logic; does not touch patients or
     global_patients. The mig 112 slot was repurposed for this new
     CHECK in Mo's review-additions batch.

Verification:
- All 4 smoke probes PASS
- RLS matrix re-run at run_no = 2.0 (post-all-four-mig): 177/177 PASS, 24
  tables covered, all outcomes match run_no = 1.6 baseline (zero policy
  regression). Mig 112's CHECK does not affect any RLS policy or any of
  the 24 matrix tables.
- 3 tsc gates + lint:scopes all clean. Gate 5 (next build × 2)
  Mac-deferred per Lesson #17 (Phase B is schema-only; no source files
  modified, so the gate is structurally clean).

Pre-flight teardown initially blocked by stale sms_reminders row
referencing test appointment 00000099-…000a0 — same class of bug as the
audit_events teardown gap mig 108 fixed. NOT a Phase B regression. One-
time hygiene applied (DELETE FROM sms_reminders WHERE appointment_id::text
LIKE '00000099%'); recurring vulnerability tracked as Phase B follow-up
B07-FU-2 in PROGRAM_STATE.md (Decision 11).

Empirical Lesson #19 codified in audits/EXECUTION_PROMPTS.md (Decision 13):
NOW() is transaction-start time, not wall-clock; smoke probes testing
BEFORE UPDATE triggers on timestamp columns must seed historical
timestamps. Pairs with Lesson #2.

Doc updates (lockstep per Lesson #13):
- ARCHITECTURE.md §3 (mig count 114→115 + highest base 111→112 + mig 109-
  112 reference) + §8.6 (mig 109-112 timeline row updated with mig 112 NEW
  description).
- DECISIONS_LOG.md D-068 amendment 2026-05-10 (B07 Phase B amendment;
  minor sharing identical to adult; capability-scoped delegation
  orthogonal to share lifecycle).
- audits/STATE_OF_WORK.md (Last updated 2026-05-10 Phase B + review-
  additions batch summary; workstream Active → Completed).
- audits/PROGRAM_STATE.md entry 18 updated (4 migs shipped); new "Post-
  Phase-B follow-up tasks (B07)" section with B07-FU-1 / B07-FU-2 /
  B07-FU-3 entries; entry 17 still marked superseded.
- audits/rls-test-matrix-reconstructed.sql header (run_no = 2.0 re-run
  note + executable v_run := 1.6 globally rewritten to 2.0 in 24 DO
  blocks + Section 0 DELETE).
- audits/EXECUTION_PROMPTS.md (NEW Empirical Lesson #19 codified).
- audits/b07-phase-b-execution-2026-05-10.md (new — 13 decisions).

Three explicit follow-ups now tracked in PROGRAM_STATE.md "Post-Phase-B
follow-up tasks (B07)" section:
1. B07-FU-1: annotate B07 architectural review §4 + §6 drift —
   `normalized_phone NOT NULL` claim wrong; "1 such case" of phone-lookup
   backfill wrong (actual is 2). Small annotation commit.
2. B07-FU-2: sms_reminders teardown gap — author a mig 113-style fix to
   extend rls_test_teardown to clean stale sms_reminders rows.
3. B07-FU-3: noah hasan non-canonical patient cleanup — Prompt 6.5
   candidate; locked sentinel gp with malformed phone.

Per REVIEW_CRITERIA.md §3 surface format. Pre-work verification per §1.1
fired STOP exceptions #1/#4 once; Mo's mid-flight rulings encoded; Phase B
resumed and completed within authority. Mo's review additions encoded in
the same commit per his "Cowork session should NOT push yet" instruction.

Phase C-E batch awaits Mo review of this commit.
